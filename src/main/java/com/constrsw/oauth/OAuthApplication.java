package com.constrsw.oauth;

// Classe preservada apos merge com origin/grupo04 (commit e21b0d9).
// A aplicacao Spring Boot ativa passou a ser br.pucrs.constrsw.oauth.OauthApplication.
// Esta classe NAO deve ser inicializada: o @SpringBootApplication foi removido
// e o main() nao e o entrypoint do Dockerfile. Componentes deste pacote
// (com.constrsw.*) tambem ficam fora do component-scan padrao, que agora
// comeca em br.pucrs.constrsw.oauth.
//
// Historico original preservado no commit 044ae20 do submodulo (stash local).
public class OAuthApplication {
}
