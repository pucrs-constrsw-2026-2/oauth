meta {
  name: 19 - Unassign Role
  type: http
  seq: 19
}

delete {
  url: {{baseUrl}}/users/{{user_id}}/roles/{{role_id}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{access_token}}
}

tests {
  test("remove atribuicao de realm role", function () {
    expect(res.getStatus()).to.equal(204);
    expect(res.getBody()).to.equal("");
  });
}
