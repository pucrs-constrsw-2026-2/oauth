# Estas variaveis ja vem do .env da RAIZ do repo (base) quando rodado via
# `docker compose up` na raiz. Este arquivo serve so pra rodar a API
# isolada (npm run start:dev) sem o compose.
OAUTH_INTERNAL_API_PORT=3001
KEYCLOAK_SERVER_URL=http://localhost:8081
KEYCLOAK_REALM=constrsw
KEYCLOAK_CLIENT_ID=oauth
KEYCLOAK_CLIENT_SECRET=wsNXUxaupU9X6jCncsn3rOEy6PDt7oJO
